# Tests

Use this folder for cross-service tests.

Suggested structure:

```text
tests/
|-- backend/   FastAPI and backend integration tests
`-- frontend/  frontend test notes or future Playwright tests
```
