# Models

Use this folder for model documentation and approved lightweight model metadata.

Do not commit large generated artifacts, private checkpoints, or regulated data.
Place local generated artifacts under `models/artifacts/`, which is ignored by git.
