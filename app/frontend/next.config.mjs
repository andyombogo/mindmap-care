/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep the starter minimal; add production headers, image domains, and rewrites here.
  reactStrictMode: true
};

export default nextConfig;
